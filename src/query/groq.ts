import { composeParentFieldQuery } from "../utils/groq";

/**
 * Helper function to compose a GROQ query for fetching the parent field of a given document.
 */
export const PARENT_FIELD_QUERY = composeParentFieldQuery();
